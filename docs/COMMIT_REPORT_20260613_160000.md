# Commit Report

## Summary

- Project: /home/beou/IdeaProjects/github-copilot-svcs
- Branch: x51-commit/20260613-160000-opencode-open-spec-scaffolding
- Mode: single project
- Timestamp: 2026-06-13 16:00:00
- Branch decision: New branch — this change set is local tooling/OpenSpec scaffolding, not part of the runtime code path, and it is safer to isolate it from main.
- Commit title: chore(opencode): add OpenSpec workflow prompts and skills
- Push target: origin/x51-commit/20260613-160000-opencode-open-spec-scaffolding

## Uncommitted Audit

- Added OpenSpec workflow prompt files under `.github/prompts/` and `.github/skills/`.
- Added command/skill scaffolding under `.opencode/commands/` and `.opencode/skills/` for the OpenSpec workflow.
- Added ignore rules for local session state (`.omo/`, `.serena/`) and updated `.gitignore` to keep the repo clean.

## Files Planned For Commit

- `.gitignore`: ignore local session/tooling state directories
- `.github/prompts/`: OpenSpec prompt definitions
- `.github/skills/`: OpenSpec skill definitions
- `.opencode/commands/`: OpenSpec command wrappers
- `.opencode/skills/`: OpenSpec command skills

## Detailed Changes

- `.gitignore`: added `.omo/` and `.serena/` ignores to prevent local agent tooling from being committed.
- `.github/prompts/`: added the OpenSpec prompt files used by the workflow (`opsx-propose`, `opsx-apply`, `opsx-archive`, `opsx-explore`).
- `.github/skills/`: added the matching OpenSpec skills for proposal/apply/archive/explore.
- `.opencode/commands/` and `.opencode/skills/`: added the same workflow scaffolding in the OpenCode command/skill layout.

## Change Content Summary

- Behavior/config/docs/tests: no runtime code behavior changed; this commit adds workflow scaffolding and repo hygiene for OpenSpec-based development.

## Affected Flows

- Developer workflow: OpenSpec tooling and prompt/skill entry points are now available from the repo.
- Local agent/session state: ignored to avoid accidental commits.

## Impact And Risks

- Impact: low risk; this is documentation/tooling scaffolding only.
- Risk: local OpenCode runtime asset directories are intentionally excluded from the commit.

## Untracked Files

- `.github/prompts/`, `.github/skills/`, `.opencode/commands/`, `.opencode/skills/`: staged for commit.
- `.omo/`, `.serena/`: ignored locally and not committed.

## Sensitive/Risk Review

- Secrets checked: none in these changes.
- Large/generated files: none.
- Deleted files: none.

## Verification

```bash
make fmt && make vet && make test && make build
```

Result:

```text
go fmt ./src/...
go vet ./src/...
go test ./src/...
ok      github.com/x51vn/github-copilot-svcs/src        5.306s
go build -ldflags="-s -w -X main.version=dev" -o github-copilot-svcs ./src
```

## Commit Comments

chore(opencode): add OpenSpec workflow prompts and skills

- Added OpenSpec prompt/skill scaffolding under `.github/` and `.opencode/`.
- Added `.gitignore` entries to keep local agent/session state out of version control.
- Verified the repo still builds and tests cleanly with `make fmt && make vet && make test && make build`.

## Notes

- The commit intentionally excludes local runtime state in `.omo/` and `.serena/`.
